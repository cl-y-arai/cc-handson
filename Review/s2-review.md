- 最初のclaude起動ディレクトリについての言及がないため、たとえば新しくディレクトリを作成するのかしないのかについては述べておくべき(新規ディレクトリの場合(blankの場合)/initコマンドCLAUDE.mdを作るために必要な情報がないことを理由に止まるので、留意が必要です。blankならばtouchで作ってしまうのが個人的にはいいかと)
- /plugin install figma@claude-plugins-official←その後、スコープを確定する必要があるが、これの記述も必要
- FigmaのURLは欲しいかもです（時間的に）
- figmaのファイル名変更ですが、（ファイル名横の ∨ メニュー → 名前を変更）左タブからファイルを選択する必要があることも表記すべき
- サブエージェントを動作させるべきディレクトリ、プロジェクトが不明瞭
- Agmsgのインストール元がsession1と異なっているので、どちらかに合わせる必要があると思います
- block-package-install.sh"等シェルスクリプトが既存のものなのかそれとも別途作成、インストールする必要があるかの記述は必要
- /code-review を実行する — 未コミットの差分に対して、バグ・エッジケース・改善点のレビューが返ってくる←git追跡は必要なので、新規ファイルに関してはステージングが必要
- レビュー指摘のうち、○番と○番を直して←コードレビューがそもそも指摘を返さなかった場合の分岐は必要かと
- gh pr create --fillは対話式なので、CCCLIのbashモードではエラーが出そう
- Session3で「README に日本語のクイックスタートを追記してほしい」という Issue を gh で作成してというghコマンドをAI側にやらせているので、PR自体もAIにやらせれば。。。とは思います(実際にsession3ではskillを作成しているので)
- example-pluginは見つからなかったので消す必要がありそうです
- section1同様#プレフィック（memory格納機能）は見つからなかったので、見直す必要はありそうです

